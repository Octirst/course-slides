# NextAuth Secret（生产环境必须使用强随机密钥）
# 生成方法：openssl rand -base64 32
AUTH_SECRET=

# Trust host for reverse proxy (Nginx)
AUTH_TRUST_HOST=true

# Student Login Password
STUDENT_PASSWORD=zbti-ai-cs2025

# Node.js 环境
NODE_ENV=production
PORT=3000

# 如果后续需要添加其他配置，在此添加
